fastapi>=0.115.0
uvicorn[standard]>=0.32.0
soundfile>=0.12.0
numpy>=1.24.0
python-multipart>=0.0.9
webrtcvad-wheels>=2.0.14  # VAD for long-audio segmentation (qwen3_asr bug A workaround)
prometheus_client>=0.20,<1  # Week 2 production hardening: /metrics exposition
python-json-logger>=2.0,<4  # Week 2 production hardening: structured JSON logging
voxedge>=0.0.4a0  # build_config_from_env required by server/core/voxedge_backend_config.py
# sherpa-onnx: installed from CUDA wheel in Dockerfile
#
# voxedge lives in its own repo. Development installs the sibling checkout
# first; images install the staged 0.0.4a0+ wheel explicitly. The requirement
# above makes the runtime API floor machine-checkable instead of silently
# accepting an older wheel that lacks build_config_from_env.
